[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "campus-errands-shared-auth"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115,<1",
    "pyjwt>=2.10,<3",
]

[tool.setuptools.packages.find]
where = ["src"]
